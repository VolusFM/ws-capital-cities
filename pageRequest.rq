select distinct ?capital ?page where 
{
	
      ?capital foaf:homepage ?page.
        


	FILTER(str(?capital) = %CAPITAL_URL%)
	#FILTER(contains(str(?capital), "http://dbpedia.org/resource/Algiers"))
}
order by ?capital
limit 1